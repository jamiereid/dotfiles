#!/usr/bin/env bash

if [[ $# -eq 1 ]]; then
    selected=$1
else
    selected=$(bat .ssh/known_hosts | cut -d\  -f 1 | uniq | fzf --border --info=inline --margin=8,20 --padding=1 --print-query)
fi

if [[ -z $selected ]]; then
    exit 0
fi

ping $selected
